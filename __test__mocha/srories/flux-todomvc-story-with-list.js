/*
   
  });

  afterEach(function() {
    console.log('si');
    //expect(todoHomePage.getTodoCount.then( r => Number(x) )).toBe(todoHomePage.getActiveListCount.then( r => Number(r) ));
  });

  it('should have proper title', function() {
    // Can be considered to be beforeAll, which Protractor lacks.
    var pageTitle = todoHomePage.getPageTitle;
    expect(pageTitle).toBe('todos');
    browser.pause();
  });


});
*/